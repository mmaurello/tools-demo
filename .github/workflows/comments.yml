name: Comment Trigger

on:
  issue_comment:
    types:
      - created

jobs:
  trigger_on_comment:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v2

    - name: Run on comment
      run: |
        echo "Pull Request comment detected!"
        # Add your custom actions or commands here